<<<<<<< HEAD
name: Deploy to Azure App Service (Container Mode)

on:
  push:
    branches:
      - 53-restructure-backend-code  # Trigger deployment on pushes to the main branch

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Step 1: Check out the code
      - name: Checkout Code
        uses: actions/checkout@v3

      # Step 2: Log in to Azure
      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      # Step 3: Log in to Azure Container Registry (ACR)
      - name: Log in to Azure Container Registry
        run: |
          az acr login --name reptrackcontainer

      # Step 4: Build and Push Docker Image to ACR
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v5
        with:
          context: .  # Path to the Dockerfile
          push: true
          tags: reptrackcontainer.azurecr.io/reptrackimage:${{ github.sha }}

      # Step 5: Deploy to Azure App Service
      - name: Deploy to Azure App Service
        uses: azure/webapps-deploy@v2
        with:
          app-name: Reptrack-main-app  # Replace with your Azure App Service name
          images: reptrackcontainer.azurecr.io/reptrackimage:${{ github.sha }}
=======
name: Django CI

on:
  push:
    branches: [ 53-restructure-backend-code ]
  pull_request:
    branches: [ 53-restructure-backend-code ]

jobs:
  build:
    runs-on: ubuntu-latest

    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
      SMTP_EMAIL: ${{ secrets.SMTP_EMAIL }}
      SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
      
     

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'  

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run migrations
      working-directory: backend
      run: |
        python manage.py migrate

    - name: Run Django tests
      working-directory: backend
      run: |
<<<<<<< HEAD
<<<<<<< HEAD
        python manage.py test
>>>>>>> 4903288 (Create main.yml)
=======
         py manage.py runserver
>>>>>>> 96ee6a4 (Update main.yml)
=======
        python manage.py test --verbosity=2
>>>>>>> 476067a (Update main.yml)

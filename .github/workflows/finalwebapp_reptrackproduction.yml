# Docs for the Azure Web Apps Deploy action: https://github.com/Azure/webapps-deploy
# More GitHub Actions for Azure: https://github.com/Azure/actions

name: Build and deploy container app to Azure Web App - reptrackproduction

on:
  push:
    branches:
      - finalwebapp
  workflow_dispatch:

jobs:
  build:
    runs-on: 'ubuntu-latest'

    steps:
    - uses: actions/checkout@v2

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to registry
      uses: docker/login-action@v2
      with:
        registry: https://reptrackfinalproduction.azurecr.io/
        username: ${{ secrets.AzureAppService_ContainerUsername_f7bba0f0fa764efc823565b743f890dc }}
        password: ${{ secrets.AzureAppService_ContainerPassword_699524bcedf74e16b115b398253b9902 }}

    - name: Build and push container image to registry
      uses: docker/build-push-action@v3
      with:
        push: true
        tags: reptrackfinalproduction.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_f7bba0f0fa764efc823565b743f890dc }}/v2.0:${{ github.sha }}
        file: ./Dockerfile

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: 'production'
      url: ${{ steps.deploy-to-webapp.outputs.webapp-url }}

    steps:
    - name: Deploy to Azure Web App
      id: deploy-to-webapp
      uses: azure/webapps-deploy@v2
      with:
        app-name: 'reptrackproduction'
        slot-name: 'production'
        publish-profile: ${{ secrets.AzureAppService_PublishProfile_96f263b7bdbd48a38988e38d652523f8 }}
        images: 'reptrackfinalproduction.azurecr.io/${{ secrets.AzureAppService_ContainerUsername_f7bba0f0fa764efc823565b743f890dc }}/v2.0:${{ github.sha }}'